$(document).ready(function(){

	// Applies the slick slide to the slidercontainer
	$('.slidercontainer').slick(
	{
	    autoplay: true,
	    arrows: false,
	    infinite: true,
	    //cssEase: 'linear',
	    speed:8000,
	    autoplaySpeed:7000,
	  	pauseOnHover:false
	});


});